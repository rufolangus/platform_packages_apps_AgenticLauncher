<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright (C) 2024 The AAOSP Project
     Licensed under the Apache License, Version 2.0 -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.agenticlauncher">

    <!-- Signature-level: only system-signed apps can submit prompts to the LLM -->
    <uses-permission android:name="android.permission.SUBMIT_LLM_REQUEST" />

    <!-- Standard launcher permissions -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgenticLauncher">

        <!-- Main launcher activity -->
        <activity
            android:name=".AgenticLauncherActivity"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:clearTaskOnLaunch="true"
            android:resumeWhilePausing="true"
            android:windowSoftInputMode="adjustPan"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

    </application>

</manifest>
